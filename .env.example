# Optional — only set when backend on Oracle Cloud is ready
# BACKEND_URL=http://YOUR-BACKEND-OCI-IP:8011
# NEXT_PUBLIC_API_BASE_URL=http://YOUR-BACKEND-OCI-IP:8011

# Leave unset to run frontend with empty data (no backend connection)
